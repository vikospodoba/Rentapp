# Инструкция по работе с изображениями и стилями

## 📸 Где загружать изображения автомобилей

### Путь для изображений:
```
rentapp/src/main/resources/static/images/
```

### Как добавить изображение:
1. Скопируй файл изображения (jpg, png, webp) в папку `rentapp/src/main/resources/static/images/`
2. Например: `rentapp/src/main/resources/static/images/toyota-camry.jpg`
3. В форме добавления/редактирования автомобиля укажи путь:
   - **Локальный файл**: `/images/toyota-camry.jpg`
   - **Внешняя ссылка**: `https://example.com/car-image.jpg`

### Пример использования в форме:
```
URL изображения: /images/toyota-camry.jpg
```

### Доступ к изображениям:
- Все файлы из `static/images/` доступны по адресу: `http://localhost:8082/images/название_файла.jpg`
- Spring Boot автоматически раздаёт статические файлы из папки `static/`

---

## 🎨 Где находятся стили страниц

### Основные CSS файлы:

1. **Главный стиль** (для всех страниц):
   ```
   rentapp/src/main/resources/static/css/style.css
   ```

2. **Стили для страниц авторизации**:
   ```
   rentapp/src/main/resources/static/css/auth.css
   ```

3. **Дополнительный стиль**:
   ```
   rentapp/src/main/resources/static/css/static.css
   ```

### Как изменить стили:

1. Открой нужный CSS файл в IntelliJ IDEA
2. Внеси изменения (цвета, шрифты, отступы и т.д.)
3. Сохрани файл
4. Перезапусти приложение или обнови страницу в браузере (Ctrl+F5 для полной перезагрузки)

### Пример изменения цвета:
```css
/* В файле style.css */
.btn-primary {
    background-color: #007bff; /* Измени на свой цвет */
    border-color: #007bff;
}
```

---

## 💰 Валюта: рубли (₽)

Все цены в приложении теперь отображаются в **рублях (₽)** вместо долларов.

### Где изменены:
- ✅ Каталог автомобилей (`cars/list.html`)
- ✅ Детали автомобиля (`cars/view.html`)
- ✅ Список аренд (`rentals/list.html`)
- ✅ Форма создания аренды (`rentals/create.html`)
- ✅ Главная страница (`home.html`)
- ✅ Профиль пользователя (`shared/profile.html`)

---

## 🗄️ База данных: PostgreSQL

### Текущая настройка:
- **В разработке**: используется **H2 in-memory** база данных (для удобства)
- **Для продакшена**: настроена поддержка **PostgreSQL**

### Как переключиться на PostgreSQL:

1. Установи PostgreSQL на компьютер
2. Создай базу данных:
   ```sql
   CREATE DATABASE rentapp;
   ```

3. Измени `application.properties`:
   ```properties
   spring.datasource.url=jdbc:postgresql://localhost:5432/rentapp
   spring.datasource.username=твой_логин
   spring.datasource.password=твой_пароль
   spring.datasource.driver-class-name=org.postgresql.Driver
   ```

4. Или используй переменные окружения:
   ```bash
   DB_URL=jdbc:postgresql://localhost:5432/rentapp
   DB_USER=твой_логин
   DB_PASSWORD=твой_пароль
   DB_DRIVER=org.postgresql.Driver
   ```

### Текущий статус:
- ✅ Зависимость PostgreSQL подключена в `pom.xml`
- ✅ H2 используется для разработки (данные не сохраняются после перезапуска)
- ✅ Для курсовой можно использовать PostgreSQL — просто измени настройки подключения





